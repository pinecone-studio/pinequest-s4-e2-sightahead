# Web framework
fastapi
uvicorn[standard]
python-dotenv
pydantic

# Auth / Database (Firebase Auth + Firestore + Storage)
firebase-admin

# Captions — PATH A (youtube_transcript_api). No Path B (yt-dlp/Whisper).
youtube-transcript-api

# Translation + Summary (OpenAI)
openai

# HTTP client (Azure TTS REST calls)
httpx

# Audio duration probing
mutagen
