fastapi
uvicorn[standard]
python-dotenv
pydantic

# Auth / Database (Firebase Auth + Firestore)
firebase-admin

# Caption
youtube-transcript-api

# Translation + Summary
openai
google-generativeai
httpx
mutagen

# Edge TTS (test endpoint /test/tts — lightweight websocket client, no ML)
edge-tts

# Audio (duration only — pydub/azure SDK not used; TTS calls Azure REST via httpx)
mutagen

# FastAPI extras
python-multipart
