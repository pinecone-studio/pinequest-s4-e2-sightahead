fastapi
uvicorn[standard]
python-dotenv
pydantic

# Auth / Database (Firebase Auth + Firestore)
firebase-admin

# Caption
youtube-transcript-api

# Translation + Summary
openai
google-generativeai
httpx
mutagen

# Audio (duration only — pydub/azure SDK not used; TTS calls Azure REST via httpx)
mutagen

# WebSocket client (Gemini 3.5 Live Translate + Azure TTS streaming)
websockets

# FastAPI extras
python-multipart
