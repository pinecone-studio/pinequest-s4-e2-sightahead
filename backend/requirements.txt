fastapi
uvicorn[standard]
python-dotenv
pydantic

# Caption / STT
youtube-transcript-api
# YouTube changes its extraction signature often; yt-dlp ships near-daily
# releases to keep up — re-pin this regularly (github.com/yt-dlp/yt-dlp/releases).
# Full extraction (nsig/PO token solving) also needs a JS runtime on the
# build host — install `deno` in the Render build step alongside this.
yt-dlp==2026.3.17

# Translation + Summary
openai
httpx

# Audio
pydub

# Database (legacy cache fallback)
sqlalchemy
psycopg2-binary

# Auth (email/password extension point)
python-jose[cryptography]
passlib[bcrypt]

# FastAPI extras
python-multipart

# TTS

# --- Tur orхисон (1GB+) ---
# pyannote.audio   <- эрэгтэй/эмэгтэй speaker detection
